import React from 'react';
import {MoviesList} from "../../Components/MoviesList/MoviesList";

const MoviesPage = () => {
    return (
        <div>
            <MoviesList/>
        </div>
    );
};

export {
    MoviesPage
};